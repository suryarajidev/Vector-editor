# Vector-editor

An easy-to-use vector editor created with Codex.

## Current prototype

The first prototype is a dependency-free web page that downloads a generated SVG file.

Open `index.html` in a browser, then select **Download a surprise SVG**. The downloaded
`tiny-submarine.svg` includes a multi-stop gradient and non-scaling strokes.
